import { ProjectTarget } from '@expo/config';
import { Reporter } from 'metro';
import { ConfigT, InputConfigT } from 'metro-config';
export interface DefaultConfigOptions {
    target?: ProjectTarget;
}
export declare function getDefaultConfig(projectRoot: string, options?: DefaultConfigOptions): InputConfigT;
export interface LoadOptions {
    config?: string;
    maxWorkers?: number;
    port?: number;
    reporter?: Reporter;
    resetCache?: boolean;
    target?: ProjectTarget;
}
export declare function loadAsync(projectRoot: string, { reporter, target, ...metroOptions }?: LoadOptions): Promise<ConfigT>;
